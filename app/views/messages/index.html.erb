<% @messages.each do |message| %>

    <div class="col-md-4">
    		<div class="message">
            <p><%= message.title %></p>
            <p><%= link_to "阅览课程", message_path(message),class: "btn btn-success" %></p>
        </div>
    </div>
    <% end %>
