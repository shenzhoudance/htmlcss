<% @messages.each do |message| %>

<div class="container">
  <div class="row">
    <div class="col-xs-4">
        <p><%= message.title %></p>
        <p><%= link_to "阅览课程", message_path(message),class: "btn btn-success" %></p>
        </div>
        <% end %>
    </div>
  </div>
</div>
